// import Request, { makeURL } from 'src/servicesS3';

// export const getBucket = (bucketName: string, region: string) => {
//   const url = makeURL(bucketName, region);

//   const opts = {
//     method: 'GET',
//     host: url,
//     path: '?list-type=2',
//     url
//   };

//   const signer = new AwsSign({
//     accessKeyId,
//     secretAccessKey
//   });
// };
